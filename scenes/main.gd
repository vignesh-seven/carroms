extends Node2D


# Called every frame. 'delta' is the elapsed time since the previous frame.
#func _integrate_forces(_delta):
	##print($Striker.linear_velocity)
	#if Input.is_action_just_pressed("start_striker"):
		##if $Striker.linear_velocity = Vector2.ZERO:
		#
		##$Striker.apply_central_impulse(-$Striker.linear_velocity)
		##$Striker.freeze = true
		##$Striker.set_physics_process(false)
		#$Striker.position = $StrikerStartMarker.position
		##$Striker.set_physics_process(true)
		##$Striker.freeze = false
